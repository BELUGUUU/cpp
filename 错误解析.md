# 五道题目的错误解析与修正记录

> 背景：OJ（cid=3775）状态显示 5 题未通过 —— **J / T / U / V / Z**，其余 28 题 AC，AC 仿真器（2552）尚未提交。
> 下面逐题给出「现象 → 根因 → 怎么改 → 代码怎么读」，修好的代码已覆盖到对应目录。
> 编译环境：`g++ -O2 -std=c++11 -Wall`，零告警。

---

## 一、J 真值表(Ⅲ)（2259）—— 行尾残缺运算符的报错位置

**现象**：WA。先做了一件事：把 Ⅲ 和已 AC 的 Ⅱ（2258）逐行 diff，结果**代码完全相同**（只差文件头注释）。
这说明「合法表达式 → 真值表」的输出部分绝对没问题（Ⅱ 已经 AC 证明过），
问题只能出在 Ⅲ 独有的**报错路径**上。

**根因**：词法分析把「写在行尾的残缺运算符」当成了普通非法词，报的是该运算符自己的位置。
但题目对「**读到末尾仍缺词**」的规定是报 **长度 + 1**（缺掉的那些字符在行尾之后）。
样例里的残缺符 `a<-b` 出现在行中间，走的是另一条分支，所以**样例永远测不出这个错**。

**修正**（`tokenize()` 里拆开两种情况）：

| 输入 | 旧输出 | 新输出 | 说明 |
|---|---|---|---|
| `a<` | at 2 | **at 3** | 行尾残缺 → 长度+1（"a<" 长 2） |
| `a<-` | at 2 | **at 4** | 行尾残缺 → 长度+1 |
| `a-` | at 2 | **at 3** | 行尾残缺 → 长度+1 |
| `a\|` | at 2 | **at 3** | 行尾残缺 → 长度+1 |
| `a<-b` | at 2 | at 2 | 行中残缺 → 词自身位置（样例行为，保持不变） |

**代码怎么读**：整体两阶段——先**整体切词**（任一位置出现词法错误就近抛出，所以「词法错误优先于语法错误」天然成立），
全部切完再**递归下降建树**。优先级由低到高 `<->` → `->` → `||` → `^` → `!` → `( )`，每层一个函数、左结合；
运算符写在操作数位置上、括号没闭合、表达式后面还有多余词，都按「第一个错在哪」定位。

---

## 二、T 24点游戏(Ⅱ)（1830）—— 循环嵌套层次错了（根因）

**现象**：WA，但样例前 12 行逐字符对得上、输出总数也正好 7680（叶子写法、括号风格、子集划分、运算符顺序全对）。

**根因**：**递归框架的嵌套层次弄反了**。题目要的次序是「运算符在最外层」：

```
gen(pool):
  for op in + - * /            ← 最外层：运算符
    for i = 1 .. num-1         ← 左子树的数字个数
      for 每个 i 元子集（按下标序）
        对左、右子树的表达式两两组合
```

旧代码把 `i` 和子集放在了运算符**外面**（`for i { for op { ... } }`）。
这两种写法**前 24 行输出完全一样，从第 25 行才开始分岔** —— 而题面样例只贴了 12 行，恰好卡在岔口之前，所以本地怎么测都是"对的"。

**实测证据**（输入 `1 2 3 4`，新旧程序对比）：

- 前 12 行：`diff` 结果为空（完全一致）
- 第 25 行：旧 = `(1+(2-(3+4)))`，新 = `(1+((2+3)+4))` ← 从这一行起分岔
- 总行数：都是 7680 ✓

**修正**：`gen()` 拆两步——先按「`i` 升序 + 子集按下标序」把所有左右子树划分**预算好并缓存**（四个运算符共用，不重复递归），
再按「运算符 → `i` → 子集 → 左右两两组合」输出。

**代码怎么读**：`num == 1` 是叶子，直接写数字；`subsets()` 枚的是**下标**子集（输入有重复数字时必须按位置划分，否则会拿错位置）；
左子树取下标集合上的数字、右子树取剩余数字**保持原次序**；每个结点都套一层括号 `(左 op 右)`。

---

## 三、U 24点游戏(Ⅲ)（1831）—— 同一个根因

**现象**：WA。题目要输出「T 中**第一个**得到 24 的表达式」，所以 T 的次序错了，这里必然跟着错。

**根因**：与 T 完全同源（枚举次序 = T 的次序）。

**修正**：`gen()` 与 T 同步改成「运算符最外层」，并保留原有的两个关键设计：

- **精确分数**求值（分子/分母约分后比较），不用 double —— 中间结果像 `1/3` 不会因为浮点误差被误判；
- 扫到第一个等于 24 的就立即返回并输出，否则 `NO`。

**验证**：`1 2 3 4` → `(1*(2*(3*4)))=24`；`12 13 4 6` → `(4*(6*(13-12)))=24`；`10 10 10 8` → `NO`（与 V 的"0 个解"互相印证）。

---

## 四、V 24点游戏(Ⅳ)（2007）—— 同一个根因

**现象**：WA。要按 T 的顺序输出每个"不同解法"的第一个表达式 + 解法总数。

**根因**：同 T 的枚举次序问题；「不同解法」的判定逻辑（6 条等价规则的规范化）**经复核是对的，不动**：

- 加减展平成「项的集合」（`a-b` 记作 `a+(-b)`），去掉 0 与 `-0`，相反项抵消 → 覆盖规则 (1)(2)(6)；
- 乘除展平成「因子的集合」（`a/b` 记作 `a×(1/b)`），去掉 1 与 `1/1`，互逆对抵消 → 覆盖规则 (3)(4)(5)；
- 排序后拼成规范串，串相同即视为同一解法；每个等价类取 T 顺序里的第一个表达式。

**修正**：只改枚举次序（与 T/U 同步）。顺手修掉两处 `-Wreorder` 告警（成员初始化顺序），恢复"零告警"。

**验证**：`5 5 5 1` → 经典唯一解 `(5*(5-(1/5)))=24`；`2 3 2 3` → 3 个解；`10 10 10 8` → 0 个解。

---

## 五、Z 自来水管道（1074）—— 数组写死上限

**现象**：WA。算法是标准的最小生成树（Kruskal + 并查集），本地样例输出 `0 17 16 26` 手算核对无误，
所以**逻辑层面没问题，是工程性隐患**。

**根因**：旧版把容量写死在代码里——`int parent[55]`、`static Edge e[5000]`。
只要某组数据的点数 `P ≥ 55` 或管道数 `R > 5000`，就会**越界写坏内存**：
轻则答案错误，重则 RE，而且在小数据上完全看不出来。

**修正**：改成**动态分配**，彻底摆脱上限：

```cpp
vector<int> parent(P + 1);      // 每个测试用例内部重置
vector<Edge>  e(R);
```

并把并查集的查找内联进主循环（路径压缩照旧，隔代压缩）。

**代码怎么读**：`P R` 逐组读入，`P = 0` 结束（`scanf` 自动跳过空白，组间空行无影响）；
边按权排序后依次取，两端点不在同一集合就合并、累加，取满 `P-1` 条即得最小总成本；`P = 1` 时无边长，答案 0。

---

## 六、复测建议（按这个顺序重交）

| 顺序 | 题号 | 目录 | 看点 |
|---|---|---|---|
| 1 | 1830 T | `26-24点2` | 输出总数 7680；第 25 行应为 `(1+((2+3)+4))`（样例只有 12 行，别只看前 12 行） |
| 2 | 1831 U | `27-24点3` | 若 T 过了，U 基本同批次过 |
| 3 | 2007 V | `28-24点4` | 解法计数 + 每个解的第一个表达式 |
| 4 | 1074 Z | `20-自来水管道` | 多点/多边的数据是否还稳 |
| 5 | 2259 J | `05-真值表` | 重点看行尾残缺运算符的报错位置 |

> 若 24 点三题仍 WA：那就是**嵌套次序又反了**（把运算符放回子集内层试一次）。
> 这两种次序前 24 行输出相同，题面样例无法区分，只能靠 OJ 结果反推 —— 换句话说，
> 只要拿到任意一组 T 的完整期望输出（哪怕是老师手上的标准答案），就能一次定死。
>
> **AC 仿真器（2552）**：目前未提交、代码未动工，需要 8 条指令的完整语义表才能做，建议等这 5 题确认过再考虑。

---

*2026-09-18 修正记录｜编译器 MinGW-w64 GCC 16.2.0*
