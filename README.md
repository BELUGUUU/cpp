# C++ 数据结构课设

湖南科技大学《数据结构与算法》课程设计 OJ 题解合集：**29 个题目目录、30 份 `main.cpp` 实现**，全部在 GCC 16.2 (MinGW-w64) 下以 `g++ -O2 -std=c++11 -Wall` 编译，**零告警**。

每份 `main.cpp` 均可直接提交 OJ；样例输出与题目逐字符一致，同一道题的多实现间做过随机对拍验证。

## 题目总览

| 目录 | 题目 | OJ 题号 | 核心算法 |
|---|---|---|---|
| `01-顺序表实现` | 单词检查·顺序表 | — | 顺序表 + 编辑距离 1 判定 |
| `02-二叉排序树实现` | 单词检查·BST | — | 迭代插入/后序释放 + index 还原字典序 |
| `03-哈希实现` | 单词检查·哈希 | — | 手写哈希（多项式 vs ELF）× 线性探测/链地址 |
| `04-线性表RDD操作` | 线性表 R/D 操作 | — | 双向链表 + 顺序表双实现，`rev` 逆转标志 |
| `05-真值表` | 真值表(Ⅲ) | 2259 | 词法分析 + 递归下降 + 错误定位 |
| `06-复杂度分析1` | 复杂度分析(Ⅰ) | 1722 | 循环次数 = C(n-1,3) 组合计数 |
| `07-复杂度分析2` | 复杂度分析(Ⅱ) | 1723 | Σ t(t+1)/2 = n(n-1)(n+1)/6 求和公式 |
| `08-Josephus1` | Josephus(Ⅰ) | 1719 | 不带头结点循环链表模拟报数 |
| `09-Josephus2` | Josephus(Ⅱ) | 2551 | 动态数组（结构体封装）删除前移 |
| `10-Josephus3` | Josephus(Ⅲ) | 1720 | n<2^31 不能模拟；k=2 公式 J(n)=2L+1 |
| `11-Josephus4` | Josephus(Ⅳ) | 1721 | 递推 f(i)=(f(i-1)+k)%i + 免取模跳跃优化 |
| `12-后缀表达式求值` | 后缀表达式求值 | 1824 | 操作数栈，逐字符扫描可解 `*8` 粘连写法 |
| `13-中缀转后缀` | 中缀转后缀 | 1823 | 经典调度场（Shunting-yard）算法 |
| `14-二叉树显示` | 二叉树的创建和显示 | 1826 | 先序串建树（`#` 为空）+ 左旋 90° 树形打印 |
| `15-表达式树输出` | 表达式树的创建与输出 | 1827 | 先序建树 → 全括号中缀表达式 |
| `16-表达式树求值` | 表达式树的值 | 1828 | 在全括号输出基础上递归求值 |
| `17-真值表1` | 真值表(Ⅰ) | 2257 | 词法切词 + 字母序变量表 |
| `18-真值表2` | 真值表(Ⅱ) | 2258 | 递归下降语法树 + 组合取值逐行求值 |
| `19-带权路径长度` | 带权路径长度 | 1832 | 哈夫曼构造即累加 WPL，小根堆 O(n log n) |
| `20-自来水管道` | 自来水管道 | 1074 | Kruskal 最小生成树 + 并查集（路径压缩+按秩合并） |
| `21-最小时间` | 最小时间 | 1075 | 补全邻接矩阵求最短路，答案 = max dist(1, j) |
| `22-修路` | 修路（2010 省赛） | 2010 | Repairing a Road：修路耗时换通行折扣的最优决策 |
| `23-推箱子BFS` | 推箱子·BFS 版 | 1981 | 状态 = (玩家, 箱子)，BFS 逐层扩展求最少步数 |
| `24-推箱子DFS` | 推箱子·DFS 版 | 1982 | 同状态空间，按 U→R→D→L 顺时针序 DFS 回溯 |
| `25-24点1` | 24点(Ⅰ) | 1829 | 表达式树先序序列（`#` 空结点）建树 |
| `26-24点2` | 24点(Ⅱ) | 1830 | 按规定递归框架枚举全部表达式树，逐行输出 |
| `27-24点3` | 24点(Ⅲ) | 1831 | 一边枚举一边求值，输出第一个 =24 的表达式 |
| `28-24点4` | 24点(Ⅳ) | 2007 | 按 6 条等价规则去重统计解法数（含交换律/负零消去） |
| `29-RepairingARoad` | Repairing a Road | 1044 | Dijkstra + 逐边枚举修路时间 t 的全局最优 |

## 两道"大题"：一题多解 + 复杂度实验

### 题目一：单词检查（拼写检查器）

字典维护 + 编辑距离为 1 的修改建议，同一道题用三种结构实现，做复杂度对比：

| 目录 | 实现 | 单次查询 | 总复杂度 |
|---|---|---|---|
| `01-顺序表实现` | 顺序表 + 单次编辑判定 | O(n·L) | O(m·n·L) |
| `02-二叉排序树实现` | BST（迭代插入/后序遍历，防退化爆栈）+ index 还原字典序 | O(L·log n) | O(m·L²·log n) |
| `03-哈希实现` | 手写哈希（多项式 vs ELF）+ 线性探测/链地址双策略 | O(L) 期望 | O(m·L) |

n ≤ 500000（字典），m ≤ 200（待查词），L ≤ 15（词长）。

**哈希实验结论**（`03-哈希实现/benchmark_result.txt`）：

- **ELF hash 低位随机性弱**：表容量取 2 的幂、`(h & mask)` 只用低位时，ELF 在线性探测下平均探测 336.84 次，补一次终混后降到 1.42——哈希函数必须与取模方式配套。
- 朴素累加哈希 2 万词规模下线性探测平均探测 18934 次，差好哈希 4 个数量级。
- 链地址法对装填因子不敏感；线性探测在 α→1 时发生一次聚集，探测长度急剧上升。

### 题目二：线性表 R/D 操作

ADT 设计 + 双实现（共用纯虚接口 `LinearList`，换实现只改一行）：

- `04-线性表RDD操作/01-双向链表实现`：双向链表 + 逆转标志
- `04-线性表RDD操作/02-顺序表实现`：数组 + `[lo,hi]` 下标窗口 + 逆转标志

**核心技巧**：`R` 不真逆置（单次 O(n)，|S|≤1e5 必 TLE），用 `rev` 标志让 R/D 都成为 O(1)；`D` 按 rev 删物理头或物理尾——这正是必须用双向链表的原因（rev 为真时删物理尾需 O(1) 拿前驱）。

**复杂度实验**（`04-线性表RDD操作/03-复杂度对比实验/benchmark_result.txt`）：规模 ×3（4000→12000）时，标志法增长 2.8 倍（线性），物理逆置法增长 11.0 倍（二次）。

## 环境与编译

- 编译器：MinGW-w64 GCC 16.2.0（WinLibs，x86_64-posix-seh）
- 编译命令：`g++ -O2 -std=c++11 -o main.exe main.cpp`
- 每个题目目录含 `sample_in.txt` 样例输入，本地验证方式：`main.exe < sample_in.txt`
